@{
    ViewData["Title"] = "Error " + ViewData["StatusCode"];
}

<div class="text-center mt-5">
    <h1 class="display-4">@ViewData["StatusCode"]</h1>
    <p class="lead">@ViewData["Message"]</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Go to Home</a>
</div>
